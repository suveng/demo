spring:
  application:
    name: istio-cloud-order-demo
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    jmx:
      exposure:
        include: *
    web:
      exposure:
        include: *
  server:
    port: 18081
server:
  port: 18080
